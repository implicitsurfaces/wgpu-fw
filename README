build with scons:

    scons

run:

    bin/main

rebuild compile_commands.json (if IDE language server is haywire):

    scons compile_commands.json

requirements:

  - geomc
  - wgpu-native
  - wgpu c++ header
  - scons

